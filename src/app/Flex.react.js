import React, { PropTypes } from 'react';
import { findDOMNode } from 'react-dom';

const Flex = React.createClass({

    getInitialState() {
        return {};
    },

    componentDidMount() {

    },

    componentWillUnmount() {

    },

    render() {
        return (
            <div className="m-wwrap m-pad white-bg" style={{minHeight: '100%'}}>
                hi
            </div>
        );
    },

});

export default Flex;
